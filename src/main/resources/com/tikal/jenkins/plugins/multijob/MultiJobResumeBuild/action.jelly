<?jelly escape-by-default='true'?>
<j:jelly xmlns:j="jelly:core" xmlns:l="/lib/layout">
    <j:if test="${action.isAvailable()}">
        <l:task icon="${action.iconFileName}" title="${action.displayName}" href="${h.getActionUrl(it.url, action)}/" post="true"/>
    </j:if>
</j:jelly>
